// Determine whether or not the passed string array is valid. A valid array is when the brackets can close evenly:
// [{([])}] === true
// [{] === false

function isValid(s) {}

console.log(isValid("[{([])}]")); // true
console.log(isValid("[{]")); // false
